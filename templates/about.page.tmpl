{{template "base" .}}

{{define "content"}}
    <div class="container">
        <div class="row">
            <h1>This is the About page</h1>
            <p>Golang is a great programming language, Developed by GooGle!</p>
            <p>Golang is a great programming language, Developed by GooGle!</p>

            <p>This came from the template: {{index .StringMap "test"}}</p>
            <p>
                {{if ne (index .StringMap "remote_ip") "" }}
                    Your Ip Address: {{index .StringMap "remote_ip"}}
                {{else}}
                    Visit home page first
                {{end}}
            </p>
        </div>
    </div>
{{end}}